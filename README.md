# Talk About Us

Talk About Us is an interactive map interface to memorialize and honor the stories of those who have lost their lives.

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`
